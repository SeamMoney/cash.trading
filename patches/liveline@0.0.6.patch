diff --git a/dist/index.cjs b/dist/index.cjs
index 5adaa70579752ec6735cb9bedf3fdcc3c1fc8244..b9c0d3eb45ae5133fbb6aff05054b299fa1fff9d 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -387,7 +387,16 @@ function blendColor(c1, c2, t) {
   if (a >= 0.995) return `rgb(${r},${g},${b})`;
   return `rgba(${r},${g},${b},${a.toFixed(3)})`;
 }
-function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlpha = 1, strokeColor) {
+function drawLinePath(ctx, pts, lineInterpolation) {
+  if (lineInterpolation === "linear") {
+    for (let i = 1; i < pts.length; i++) {
+      ctx.lineTo(pts[i][0], pts[i][1]);
+    }
+    return;
+  }
+  drawSpline(ctx, pts);
+}
+function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlpha = 1, strokeColor, lineInterpolation = "smooth") {
   const { h, pad } = layout;
   const baseAlpha = ctx.globalAlpha;
   if (showFill && fillAlpha > 0.01) {
@@ -398,7 +407,7 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
     ctx.beginPath();
     ctx.moveTo(pts[0][0], h - pad.bottom);
     ctx.lineTo(pts[0][0], pts[0][1]);
-    drawSpline(ctx, pts);
+    drawLinePath(ctx, pts, lineInterpolation);
     ctx.lineTo(pts[pts.length - 1][0], h - pad.bottom);
     ctx.closePath();
     ctx.fillStyle = grad;
@@ -407,7 +416,7 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
   ctx.globalAlpha = baseAlpha * lineAlpha;
   ctx.beginPath();
   ctx.moveTo(pts[0][0], pts[0][1]);
-  drawSpline(ctx, pts);
+  drawLinePath(ctx, pts, lineInterpolation);
   ctx.strokeStyle = strokeColor ?? palette.line;
   ctx.lineWidth = palette.lineWidth;
   ctx.lineJoin = "round";
@@ -415,7 +424,7 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
   ctx.stroke();
   ctx.globalAlpha = baseAlpha;
 }
-function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scrubX, scrubAmount = 0, chartReveal = 1, now_ms = 0, colorBlend = 1, skipDashLine = false, fillScale = 1) {
+function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scrubX, scrubAmount = 0, chartReveal = 1, now_ms = 0, colorBlend = 1, skipDashLine = false, fillScale = 1, lineInterpolation = "smooth") {
   const { h, pad, toX, toY, chartW, chartH } = layout;
   const incomingAlpha = ctx.globalAlpha;
   const yMin = pad.top;
@@ -431,9 +440,9 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
     const baseY = loadingY(t, centerY, amplitude, scroll);
     return baseY + (rawY - baseY) * localReveal;
   } : (rawY, _x) => rawY;
-  const pts = visible.map((p, i) => {
+  const pts = visible.map((p) => {
     const x = toX(p.time);
-    const y = i === visible.length - 1 ? morphY(clampY(toY(smoothValue)), x) : morphY(clampY(toY(p.value)), x);
+    const y = morphY(clampY(toY(p.value)), x);
     return [x, y];
   });
   const liveTipX = toX(now);
@@ -460,17 +469,17 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
     ctx.beginPath();
     ctx.rect(0, 0, scrubX, h);
     ctx.clip();
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, lineInterpolation);
     ctx.restore();
     ctx.save();
     ctx.beginPath();
     ctx.rect(scrubX, 0, layout.w - scrubX, h);
     ctx.clip();
     ctx.globalAlpha = incomingAlpha * (1 - scrubAmount * 0.6);
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, lineInterpolation);
     ctx.restore();
   } else {
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, lineInterpolation);
   }
   ctx.restore();
   if (!skipDashLine) {
@@ -1497,7 +1506,7 @@ function drawFrame(ctx, layout, palette, opts) {
     ctx.restore();
   }
   const scrubX = opts.scrubAmount > 0.05 ? opts.hoverX : null;
-  const pts = drawLine(ctx, layout, palette, opts.visible, opts.smoothValue, opts.now, opts.showFill, scrubX, opts.scrubAmount, reveal, opts.now_ms);
+  const pts = drawLine(ctx, layout, palette, opts.visible, opts.smoothValue, opts.now, opts.showFill, scrubX, opts.scrubAmount, reveal, opts.now_ms, 1, false, 1, opts.lineInterpolation);
   {
     const timeAlpha = reveal < 1 ? revealRamp(0.15, 0.7) : 1;
     if (timeAlpha > 0.01) {
@@ -2036,7 +2045,10 @@ function updateWindowTransition(cfg, wt, displayWindow, displayMin, displayMax,
   }
   let windowTransProgress = 0;
   let resultWindow;
-  if (noMotion || wt.startMs === 0) {
+  if (noMotion) {
+    resultWindow = cfg.windowSecs;
+    wt.startMs = 0;
+  } else if (wt.startMs === 0) {
     resultWindow = cfg.windowSecs;
   } else {
     const elapsed = now_ms - wt.startMs;
@@ -2177,8 +2189,10 @@ function updateBadgeDOM(badge, cfg, smoothValue, layout, momentum, badgeY, badge
     const badgeSpeed = isWindowTransitioning ? BADGE_Y_LERP_TRANSITIONING : BADGE_Y_LERP;
     badgeY = lerp(badgeY, targetBadgeY, badgeSpeed, dt);
   }
-  const badgeLeft = w - pad.right + 8 - BADGE_PAD_X - tailLen;
-  const badgeTop = badgeY - pillH / 2;
+  const badgeLeft = cfg.badgeInside
+    ? w - pad.right - totalW - 8
+    : w - pad.right + 8 - BADGE_PAD_X - tailLen;
+  const badgeTop = Math.max(0, Math.min(h - pillH, badgeY - pillH / 2 + cfg.badgeOffsetY));
   badge.container.style.transform = `translate3d(${badgeLeft}px, ${badgeTop}px, 0)`;
   if (cfg.badgeVariant === "minimal") {
     badge.path.setAttribute("fill", cfg.palette.badgeOuterBg);
@@ -2433,6 +2447,8 @@ function useLivelineEngine(canvasRef, containerRef, config) {
   (0, import_react.useEffect)(() => {
     const container = containerRef.current;
     if (!container) return;
+    let touchStart = null;
+    let touchIntent = "pending";
     const onMove = (e) => {
       if (!configRef.current.scrub) return;
       const rect = container.getBoundingClientRect();
@@ -2445,17 +2461,39 @@ function useLivelineEngine(canvasRef, containerRef, config) {
     const onTouchStart = (e) => {
       if (!configRef.current.scrub) return;
       if (e.touches.length !== 1) return;
+      touchStart = { x: e.touches[0].clientX, y: e.touches[0].clientY };
+      touchIntent = "pending";
       const rect = container.getBoundingClientRect();
       hoverXRef.current = e.touches[0].clientX - rect.left;
     };
     const onTouchMove = (e) => {
       if (!configRef.current.scrub) return;
       if (e.touches.length !== 1) return;
-      e.preventDefault();
+      const touch = e.touches[0];
+      if (!touchStart) {
+        touchStart = { x: touch.clientX, y: touch.clientY };
+      }
+      if (touchIntent === "pending") {
+        const deltaX = touch.clientX - touchStart.x;
+        const deltaY = touch.clientY - touchStart.y;
+        if (Math.max(Math.abs(deltaX), Math.abs(deltaY)) >= 7) {
+          touchIntent = Math.abs(deltaY) > Math.abs(deltaX) * 1.15
+            ? "vertical"
+            : "horizontal";
+        }
+      }
+      if (touchIntent === "vertical") {
+        hoverXRef.current = null;
+        configRef.current.onHover?.(null);
+        return;
+      }
+      if (touchIntent === "horizontal") e.preventDefault();
       const rect = container.getBoundingClientRect();
-      hoverXRef.current = e.touches[0].clientX - rect.left;
+      hoverXRef.current = touch.clientX - rect.left;
     };
     const onTouchEnd = () => {
+      touchStart = null;
+      touchIntent = "pending";
       hoverXRef.current = null;
       configRef.current.onHover?.(null);
     };
@@ -2526,7 +2564,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       return;
     }
     applyDpr(ctx, dpr, w, h);
-    const noMotion = reducedMotionRef.current;
+    const noMotion = reducedMotionRef.current || cfg.animated === false;
     const isCandle = cfg.mode === "candle";
     if (isCandle) {
       if (cfg.paused && pausedCandlesRef.current === null && (cfg.candles?.length ?? 0) > 0) {
@@ -2585,12 +2623,14 @@ function useLivelineEngine(canvasRef, containerRef, config) {
     if (loadingAlphaRef.current > 0.99) loadingAlphaRef.current = 1;
     const loadingAlpha = loadingAlphaRef.current;
     const revealTarget = !cfg.loading && hasData ? 1 : 0;
-    chartRevealRef.current = noMotion ? revealTarget : lerp(
-      chartRevealRef.current,
-      revealTarget,
-      revealTarget === 1 ? CHART_REVEAL_SPEED_FWD : CHART_REVEAL_SPEED,
-      dt
-    );
+    chartRevealRef.current = cfg.animateInitial === false
+      ? revealTarget
+      : noMotion ? revealTarget : lerp(
+        chartRevealRef.current,
+        revealTarget,
+        revealTarget === 1 ? CHART_REVEAL_SPEED_FWD : CHART_REVEAL_SPEED,
+        dt
+      );
     if (Math.abs(chartRevealRef.current - revealTarget) < 5e-3) {
       chartRevealRef.current = revealTarget;
     }
@@ -3530,6 +3570,8 @@ function Liveline({
   degen: degenProp,
   badgeTail = true,
   badgeVariant = "default",
+  badgeInside = false,
+  badgeOffsetY = 0,
   showValue = false,
   valueMomentumColor = false,
   windows,
@@ -3556,6 +3598,9 @@ function Liveline({
   onModeChange,
   onSeriesToggle,
   seriesToggleCompact = false,
+  lineInterpolation = "smooth",
+  animateInitial = true,
+  animated = true,
   className,
   style
 }) {
@@ -3669,6 +3714,8 @@ function Liveline({
     degenOptions: isMultiSeries ? void 0 : degenOptions,
     badgeTail,
     badgeVariant,
+    badgeInside,
+    badgeOffsetY,
     tooltipY,
     tooltipOutline,
     valueMomentumColor,
@@ -3686,7 +3733,10 @@ function Liveline({
     lineValue,
     multiSeries,
     isMultiSeries,
-    hiddenSeriesIds: hiddenSeries
+    hiddenSeriesIds: hiddenSeries,
+    lineInterpolation,
+    animateInitial,
+    animated
   });
   const cursorStyle = scrub ? cursor : "default";
   const activeColor = isDark ? "rgba(255,255,255,0.7)" : "rgba(0,0,0,0.55)";
diff --git a/dist/index.d.cts b/dist/index.d.cts
index c5c3f609f858b875c5564d616ddf61e99ee9c5fa..000c13debaea50788c0a65a3b9419932c9119d6d 100644
--- a/dist/index.d.cts
+++ b/dist/index.d.cts
@@ -70,6 +70,10 @@ interface LivelineProps {
     onWindowChange?: (secs: number) => void;
     windowStyle?: WindowStyle;
     badgeVariant?: BadgeVariant;
+    /** Place the live value badge inside the plot instead of reserving a right axis gutter. */
+    badgeInside?: boolean;
+    /** Move the badge vertically without moving the live point or guide line. */
+    badgeOffsetY?: number;
     tooltipY?: number;
     tooltipOutline?: boolean;
     orderbook?: OrderbookData;
@@ -91,6 +95,12 @@ interface LivelineProps {
     onModeChange?: (mode: 'line' | 'candle') => void;
     onSeriesToggle?: (id: string, visible: boolean) => void;
     seriesToggleCompact?: boolean;
+    /** Join observed points directly instead of applying a visual spline. */
+    lineInterpolation?: 'smooth' | 'linear';
+    /** Animate the synthetic loading shape into the first real series. */
+    animateInitial?: boolean;
+    /** Animate values, scales, and viewport transitions. */
+    animated?: boolean;
     className?: string;
     style?: CSSProperties;
 }
@@ -102,7 +112,7 @@ interface CandlePoint {
     close: number;
 }
 
-declare function Liveline({ data, value, series: seriesProp, theme, color, window: windowSecs, grid, badge, momentum, fill, scrub, loading, paused, emptyText, exaggerate, degen: degenProp, badgeTail, badgeVariant, showValue, valueMomentumColor, windows, onWindowChange, windowStyle, tooltipY, tooltipOutline, orderbook, referenceLine, formatValue, formatTime, lerpSpeed, padding: paddingOverride, onHover, cursor, pulse, mode, candles, candleWidth, liveCandle, lineMode, lineData, lineValue, onModeChange, onSeriesToggle, seriesToggleCompact, className, style, }: LivelineProps): react_jsx_runtime.JSX.Element;
+declare function Liveline({ data, value, series: seriesProp, theme, color, window: windowSecs, grid, badge, momentum, fill, scrub, loading, paused, emptyText, exaggerate, degen: degenProp, badgeTail, badgeVariant, badgeInside, badgeOffsetY, showValue, valueMomentumColor, windows, onWindowChange, windowStyle, tooltipY, tooltipOutline, orderbook, referenceLine, formatValue, formatTime, lerpSpeed, padding: paddingOverride, onHover, cursor, pulse, mode, candles, candleWidth, liveCandle, lineMode, lineData, lineValue, onModeChange, onSeriesToggle, seriesToggleCompact, lineInterpolation, animateInitial, animated, className, style, }: LivelineProps): react_jsx_runtime.JSX.Element;
 
 interface LivelineTransitionProps {
     /** Key of the active child to display. Must match a child's `key` prop. */
diff --git a/dist/index.d.ts b/dist/index.d.ts
index c5c3f609f858b875c5564d616ddf61e99ee9c5fa..000c13debaea50788c0a65a3b9419932c9119d6d 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -70,6 +70,10 @@ interface LivelineProps {
     onWindowChange?: (secs: number) => void;
     windowStyle?: WindowStyle;
     badgeVariant?: BadgeVariant;
+    /** Place the live value badge inside the plot instead of reserving a right axis gutter. */
+    badgeInside?: boolean;
+    /** Move the badge vertically without moving the live point or guide line. */
+    badgeOffsetY?: number;
     tooltipY?: number;
     tooltipOutline?: boolean;
     orderbook?: OrderbookData;
@@ -91,6 +95,12 @@ interface LivelineProps {
     onModeChange?: (mode: 'line' | 'candle') => void;
     onSeriesToggle?: (id: string, visible: boolean) => void;
     seriesToggleCompact?: boolean;
+    /** Join observed points directly instead of applying a visual spline. */
+    lineInterpolation?: 'smooth' | 'linear';
+    /** Animate the synthetic loading shape into the first real series. */
+    animateInitial?: boolean;
+    /** Animate values, scales, and viewport transitions. */
+    animated?: boolean;
     className?: string;
     style?: CSSProperties;
 }
@@ -102,7 +112,7 @@ interface CandlePoint {
     close: number;
 }
 
-declare function Liveline({ data, value, series: seriesProp, theme, color, window: windowSecs, grid, badge, momentum, fill, scrub, loading, paused, emptyText, exaggerate, degen: degenProp, badgeTail, badgeVariant, showValue, valueMomentumColor, windows, onWindowChange, windowStyle, tooltipY, tooltipOutline, orderbook, referenceLine, formatValue, formatTime, lerpSpeed, padding: paddingOverride, onHover, cursor, pulse, mode, candles, candleWidth, liveCandle, lineMode, lineData, lineValue, onModeChange, onSeriesToggle, seriesToggleCompact, className, style, }: LivelineProps): react_jsx_runtime.JSX.Element;
+declare function Liveline({ data, value, series: seriesProp, theme, color, window: windowSecs, grid, badge, momentum, fill, scrub, loading, paused, emptyText, exaggerate, degen: degenProp, badgeTail, badgeVariant, badgeInside, badgeOffsetY, showValue, valueMomentumColor, windows, onWindowChange, windowStyle, tooltipY, tooltipOutline, orderbook, referenceLine, formatValue, formatTime, lerpSpeed, padding: paddingOverride, onHover, cursor, pulse, mode, candles, candleWidth, liveCandle, lineMode, lineData, lineValue, onModeChange, onSeriesToggle, seriesToggleCompact, lineInterpolation, animateInitial, animated, className, style, }: LivelineProps): react_jsx_runtime.JSX.Element;
 
 interface LivelineTransitionProps {
     /** Key of the active child to display. Must match a child's `key` prop. */
diff --git a/dist/index.js b/dist/index.js
index 78ec748aeb09db24ee2a5a09030d55a410b8b1b1..361c36a7e76af13aff1bd908298dd801e5dfa907 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -360,7 +360,16 @@ function blendColor(c1, c2, t) {
   if (a >= 0.995) return `rgb(${r},${g},${b})`;
   return `rgba(${r},${g},${b},${a.toFixed(3)})`;
 }
-function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlpha = 1, strokeColor) {
+function drawLinePath(ctx, pts, lineInterpolation) {
+  if (lineInterpolation === "linear") {
+    for (let i = 1; i < pts.length; i++) {
+      ctx.lineTo(pts[i][0], pts[i][1]);
+    }
+    return;
+  }
+  drawSpline(ctx, pts);
+}
+function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlpha = 1, strokeColor, lineInterpolation = "smooth") {
   const { h, pad } = layout;
   const baseAlpha = ctx.globalAlpha;
   if (showFill && fillAlpha > 0.01) {
@@ -371,7 +380,7 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
     ctx.beginPath();
     ctx.moveTo(pts[0][0], h - pad.bottom);
     ctx.lineTo(pts[0][0], pts[0][1]);
-    drawSpline(ctx, pts);
+    drawLinePath(ctx, pts, lineInterpolation);
     ctx.lineTo(pts[pts.length - 1][0], h - pad.bottom);
     ctx.closePath();
     ctx.fillStyle = grad;
@@ -380,7 +389,7 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
   ctx.globalAlpha = baseAlpha * lineAlpha;
   ctx.beginPath();
   ctx.moveTo(pts[0][0], pts[0][1]);
-  drawSpline(ctx, pts);
+  drawLinePath(ctx, pts, lineInterpolation);
   ctx.strokeStyle = strokeColor ?? palette.line;
   ctx.lineWidth = palette.lineWidth;
   ctx.lineJoin = "round";
@@ -388,7 +397,7 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
   ctx.stroke();
   ctx.globalAlpha = baseAlpha;
 }
-function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scrubX, scrubAmount = 0, chartReveal = 1, now_ms = 0, colorBlend = 1, skipDashLine = false, fillScale = 1) {
+function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scrubX, scrubAmount = 0, chartReveal = 1, now_ms = 0, colorBlend = 1, skipDashLine = false, fillScale = 1, lineInterpolation = "smooth") {
   const { h, pad, toX, toY, chartW, chartH } = layout;
   const incomingAlpha = ctx.globalAlpha;
   const yMin = pad.top;
@@ -404,9 +413,9 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
     const baseY = loadingY(t, centerY, amplitude, scroll);
     return baseY + (rawY - baseY) * localReveal;
   } : (rawY, _x) => rawY;
-  const pts = visible.map((p, i) => {
+  const pts = visible.map((p) => {
     const x = toX(p.time);
-    const y = i === visible.length - 1 ? morphY(clampY(toY(smoothValue)), x) : morphY(clampY(toY(p.value)), x);
+    const y = morphY(clampY(toY(p.value)), x);
     return [x, y];
   });
   const liveTipX = toX(now);
@@ -433,17 +442,17 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
     ctx.beginPath();
     ctx.rect(0, 0, scrubX, h);
     ctx.clip();
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, lineInterpolation);
     ctx.restore();
     ctx.save();
     ctx.beginPath();
     ctx.rect(scrubX, 0, layout.w - scrubX, h);
     ctx.clip();
     ctx.globalAlpha = incomingAlpha * (1 - scrubAmount * 0.6);
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, lineInterpolation);
     ctx.restore();
   } else {
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, lineInterpolation);
   }
   ctx.restore();
   if (!skipDashLine) {
@@ -1470,7 +1479,7 @@ function drawFrame(ctx, layout, palette, opts) {
     ctx.restore();
   }
   const scrubX = opts.scrubAmount > 0.05 ? opts.hoverX : null;
-  const pts = drawLine(ctx, layout, palette, opts.visible, opts.smoothValue, opts.now, opts.showFill, scrubX, opts.scrubAmount, reveal, opts.now_ms);
+  const pts = drawLine(ctx, layout, palette, opts.visible, opts.smoothValue, opts.now, opts.showFill, scrubX, opts.scrubAmount, reveal, opts.now_ms, 1, false, 1, opts.lineInterpolation);
   {
     const timeAlpha = reveal < 1 ? revealRamp(0.15, 0.7) : 1;
     if (timeAlpha > 0.01) {
@@ -2009,7 +2018,10 @@ function updateWindowTransition(cfg, wt, displayWindow, displayMin, displayMax,
   }
   let windowTransProgress = 0;
   let resultWindow;
-  if (noMotion || wt.startMs === 0) {
+  if (noMotion) {
+    resultWindow = cfg.windowSecs;
+    wt.startMs = 0;
+  } else if (wt.startMs === 0) {
     resultWindow = cfg.windowSecs;
   } else {
     const elapsed = now_ms - wt.startMs;
@@ -2150,8 +2162,10 @@ function updateBadgeDOM(badge, cfg, smoothValue, layout, momentum, badgeY, badge
     const badgeSpeed = isWindowTransitioning ? BADGE_Y_LERP_TRANSITIONING : BADGE_Y_LERP;
     badgeY = lerp(badgeY, targetBadgeY, badgeSpeed, dt);
   }
-  const badgeLeft = w - pad.right + 8 - BADGE_PAD_X - tailLen;
-  const badgeTop = badgeY - pillH / 2;
+  const badgeLeft = cfg.badgeInside
+    ? w - pad.right - totalW - 8
+    : w - pad.right + 8 - BADGE_PAD_X - tailLen;
+  const badgeTop = Math.max(0, Math.min(h - pillH, badgeY - pillH / 2 + cfg.badgeOffsetY));
   badge.container.style.transform = `translate3d(${badgeLeft}px, ${badgeTop}px, 0)`;
   if (cfg.badgeVariant === "minimal") {
     badge.path.setAttribute("fill", cfg.palette.badgeOuterBg);
@@ -2406,6 +2420,8 @@ function useLivelineEngine(canvasRef, containerRef, config) {
   useEffect(() => {
     const container = containerRef.current;
     if (!container) return;
+    let touchStart = null;
+    let touchIntent = "pending";
     const onMove = (e) => {
       if (!configRef.current.scrub) return;
       const rect = container.getBoundingClientRect();
@@ -2418,17 +2434,39 @@ function useLivelineEngine(canvasRef, containerRef, config) {
     const onTouchStart = (e) => {
       if (!configRef.current.scrub) return;
       if (e.touches.length !== 1) return;
+      touchStart = { x: e.touches[0].clientX, y: e.touches[0].clientY };
+      touchIntent = "pending";
       const rect = container.getBoundingClientRect();
       hoverXRef.current = e.touches[0].clientX - rect.left;
     };
     const onTouchMove = (e) => {
       if (!configRef.current.scrub) return;
       if (e.touches.length !== 1) return;
-      e.preventDefault();
+      const touch = e.touches[0];
+      if (!touchStart) {
+        touchStart = { x: touch.clientX, y: touch.clientY };
+      }
+      if (touchIntent === "pending") {
+        const deltaX = touch.clientX - touchStart.x;
+        const deltaY = touch.clientY - touchStart.y;
+        if (Math.max(Math.abs(deltaX), Math.abs(deltaY)) >= 7) {
+          touchIntent = Math.abs(deltaY) > Math.abs(deltaX) * 1.15
+            ? "vertical"
+            : "horizontal";
+        }
+      }
+      if (touchIntent === "vertical") {
+        hoverXRef.current = null;
+        configRef.current.onHover?.(null);
+        return;
+      }
+      if (touchIntent === "horizontal") e.preventDefault();
       const rect = container.getBoundingClientRect();
-      hoverXRef.current = e.touches[0].clientX - rect.left;
+      hoverXRef.current = touch.clientX - rect.left;
     };
     const onTouchEnd = () => {
+      touchStart = null;
+      touchIntent = "pending";
       hoverXRef.current = null;
       configRef.current.onHover?.(null);
     };
@@ -2499,7 +2537,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       return;
     }
     applyDpr(ctx, dpr, w, h);
-    const noMotion = reducedMotionRef.current;
+    const noMotion = reducedMotionRef.current || cfg.animated === false;
     const isCandle = cfg.mode === "candle";
     if (isCandle) {
       if (cfg.paused && pausedCandlesRef.current === null && (cfg.candles?.length ?? 0) > 0) {
@@ -2558,12 +2596,14 @@ function useLivelineEngine(canvasRef, containerRef, config) {
     if (loadingAlphaRef.current > 0.99) loadingAlphaRef.current = 1;
     const loadingAlpha = loadingAlphaRef.current;
     const revealTarget = !cfg.loading && hasData ? 1 : 0;
-    chartRevealRef.current = noMotion ? revealTarget : lerp(
-      chartRevealRef.current,
-      revealTarget,
-      revealTarget === 1 ? CHART_REVEAL_SPEED_FWD : CHART_REVEAL_SPEED,
-      dt
-    );
+    chartRevealRef.current = cfg.animateInitial === false
+      ? revealTarget
+      : noMotion ? revealTarget : lerp(
+        chartRevealRef.current,
+        revealTarget,
+        revealTarget === 1 ? CHART_REVEAL_SPEED_FWD : CHART_REVEAL_SPEED,
+        dt
+      );
     if (Math.abs(chartRevealRef.current - revealTarget) < 5e-3) {
       chartRevealRef.current = revealTarget;
     }
@@ -3503,6 +3543,8 @@ function Liveline({
   degen: degenProp,
   badgeTail = true,
   badgeVariant = "default",
+  badgeInside = false,
+  badgeOffsetY = 0,
   showValue = false,
   valueMomentumColor = false,
   windows,
@@ -3529,6 +3571,9 @@ function Liveline({
   onModeChange,
   onSeriesToggle,
   seriesToggleCompact = false,
+  lineInterpolation = "smooth",
+  animateInitial = true,
+  animated = true,
   className,
   style
 }) {
@@ -3642,6 +3687,8 @@ function Liveline({
     degenOptions: isMultiSeries ? void 0 : degenOptions,
     badgeTail,
     badgeVariant,
+    badgeInside,
+    badgeOffsetY,
     tooltipY,
     tooltipOutline,
     valueMomentumColor,
@@ -3659,7 +3706,10 @@ function Liveline({
     lineValue,
     multiSeries,
     isMultiSeries,
-    hiddenSeriesIds: hiddenSeries
+    hiddenSeriesIds: hiddenSeries,
+    lineInterpolation,
+    animateInitial,
+    animated
   });
   const cursorStyle = scrub ? cursor : "default";
   const activeColor = isDark ? "rgba(255,255,255,0.7)" : "rgba(0,0,0,0.55)";
