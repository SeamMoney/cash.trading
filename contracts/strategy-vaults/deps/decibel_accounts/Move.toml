# Compile-time interface mirroring the on-chain `decibel_accounts` package, which owns
# dex_accounts. Points at the CURRENT Decibel package 0xe7da27…b7f (verified 2026-07-30).
# The old 0x952535…be9f has no delegation revocation at all — see docs/CURATOR-RULES.md §2.
# Name MUST match on-chain. Stubs only; never published.
[package]
name = "decibel_accounts"
version = "0.1.0"

[addresses]
decibel = "0xe7da2794b1d8af76532ed95f38bfdf1136abfd8ea3a240189971988a83101b7f"

[dependencies]
AptosFramework = { git = "https://github.com/aptos-labs/aptos-core.git", subdir = "aptos-move/framework/aptos-framework", rev = "mainnet" }
decibel_perp_dex = { local = "../decibel_perp_dex" }
AptosTrading = { local = "../AptosTrading" }
