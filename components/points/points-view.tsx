"use client"

import { PointsPageClient } from "@/components/points/points-page-client"

/** Embedded variant kept for /automation, which is being rebuilt separately. */
export function PointsView() {
  return <PointsPageClient embedded />
}
