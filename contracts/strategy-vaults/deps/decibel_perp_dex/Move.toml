# Compile-time interface mirroring the on-chain `decibel_perp_dex` package, which owns
# perp_order / perp_market / perp_engine / builder_code_registry. Points at the CURRENT
# Decibel package 0xe7da27…b7f (verified 2026-07-30). Name MUST match on-chain. Stubs only.
[package]
name = "decibel_perp_dex"
version = "0.1.0"

[addresses]
decibel = "_"

[dependencies]
AptosFramework = { git = "https://github.com/aptos-labs/aptos-core.git", subdir = "aptos-move/framework/aptos-framework", rev = "mainnet" }
AptosTrading = { local = "../AptosTrading" }
